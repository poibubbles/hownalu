In the cloud, you don't *install* - you *deploy*.

The cloud service provider determines the boot procedure - not Linux.

For RHCSA you need access to a GRUB boot menu. So no use cloud for learning
RHCSA.
